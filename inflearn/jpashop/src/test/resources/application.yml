spring:
#  datasource:
#    url: jdbc:h2:mem:testdb  //메모리 모드로 동작
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create  // create-drop 은 종료시점에 drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
#스프링 부트에서 datasource에 대한 설정이 없으면 기본적으로 메모리 DB 사용

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace